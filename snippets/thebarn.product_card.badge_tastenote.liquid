{% comment %}
    Display by adding 

    {%render 'thebarn.product_card.badge_tastenote' , card_product:card_product , show_tastenotesr:show_tastenotes %}
{%endcomment%}

{% if show_tastenotes == false%}
    <div class="card__50_50__grid">
    {% case card_product.type %}
       {% when "Espresso Coffee" or "Filter Coffee"%}
        {% assign tastenotes = card_product.metafields.custom.taste_notes | split: "." %}
       <div class = "card_tastnote_text" style="grid-area: a;"> <p style= "height:fit-content; margin: 0px;"> {{ tastenotes.first |downcase}} </p> </div>
       <div class = "card_tastnote_text"  style="grid-area: b;"> <p style= "height:fit-content; margin: 0px;"> {{ tastenotes[1] |downcase}} </p> </div>
       {% else %}
         <p class="card_tastnote_text"style="text-transform: capitalize; grid-area: a; grid-column: span 2;"> {{ card_product.metafields.custom.taste_notes|escape |downcase }}</p>
     {%endcase%}
   </div>
   
{% endif %}   

